import type { CreateCatalogPayload } from "./types";

/** Catálogos opcionales creados al primer acceso (vacío: bancos van por normalización OCR). */
export const DEFAULT_CATALOGS: CreateCatalogPayload[] = [];
